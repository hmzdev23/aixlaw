# Optional. Without these the app still runs (mock translations + canned debate).
ANTHROPIC_API_KEY=

# Optional. If absent, /api/calendar/book returns a Google Calendar template URL
# the user can click to add the event manually.
GOOGLE_OAUTH_TOKEN=

# Optional. If absent, /api/slack/post returns a stub success.
SLACK_WEBHOOK_URL=
