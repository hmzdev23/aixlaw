export default function HomePage() {
  return (
    <main style={{ padding: 32, fontFamily: "system-ui, sans-serif" }}>
      <h1>Gambit (Sprint 0 placeholder)</h1>
      <p>
        T1 (Hamza) replaces this page with the marketing shell + Sarah cold open.
        T4 engine endpoints are live under <code>/api/engine/*</code>.
      </p>
    </main>
  );
}
