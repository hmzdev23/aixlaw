# Stack lock (consolidation)

Per Gambit consolidation plan:

- **Next.js:** `15.5.4`
- **React / React DOM:** `19.0.0`
- **Tailwind:** v4 (PostCSS pipeline)
- **TypeScript:** strict
- **Package manager:** `pnpm`

Bump only with team agreement and regression pass (`pnpm typecheck && pnpm lint && pnpm test && pnpm build`).
